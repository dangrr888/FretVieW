#ifndef GUARD_MINOR_H
#define GUARD_MINOR_H

//functions returning the fret numbers
//associated with minor scales of a 
//specified key

#include <vector>
#include <string>

const std::vector<std::string>& Min_Ab();
const std::vector<std::string>& Min_A();
const std::vector<std::string>& Min_As();
const std::vector<std::string>& Min_Bb();
const std::vector<std::string>& Min_B();
const std::vector<std::string>& Min_Bs();
const std::vector<std::string>& Min_Cb();
const std::vector<std::string>& Min_C();
const std::vector<std::string>& Min_Cs();
const std::vector<std::string>& Min_Db();
const std::vector<std::string>& Min_D();
const std::vector<std::string>& Min_Ds();
const std::vector<std::string>& Min_Eb();
const std::vector<std::string>& Min_E();
const std::vector<std::string>& Min_Es();
const std::vector<std::string>& Min_Fb();
const std::vector<std::string>& Min_F();
const std::vector<std::string>& Min_Fs();
const std::vector<std::string>& Min_Gb();
const std::vector<std::string>& Min_G();
const std::vector<std::string>& Min_Gs();

#endif

